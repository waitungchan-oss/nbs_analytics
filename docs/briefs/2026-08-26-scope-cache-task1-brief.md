# Scope-aware cache generation signature — Task 1

建立 core revenue semantic signature、SQLite file signature diagnostics、v2 cache metadata 與 v1 migration fallback 的 failing contract tests。

範圍僅限測試契約與 temporary SQLite fixtures；不修改正式 SQLite、baseline、revenue rules、export schema 或 Git。

正式口徑維持「不含掛賬核銷與TT退款轉團款」，2026-05 frozen baseline 維持 HKD 12,057,968。
